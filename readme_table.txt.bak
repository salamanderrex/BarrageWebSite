create table if not exists av (
id int primary key auto_increment,

ptime int default 0)



create table av (
id int primary key auto_increment,
source varchar(10),
vid varchar(30),
title varchar(10),
playtime int default 0
)